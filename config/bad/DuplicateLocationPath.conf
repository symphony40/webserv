server {
    listen 8080
    server_name duplicatelocation.com
    root /var/www/html

    route / {
        root /var/www/html
        index index.html
    }

    route / {
        allow POST
    }
}
